{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "250mb"
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.ts" }
  ]
}
